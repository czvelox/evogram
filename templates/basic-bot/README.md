# Basic bot
This is a basic example of a Telegram bot built using Evogram framework. The bot includes a single command "start" and comes with a pre-configured project setup to quickly start developing.

## Install
To install the template, open the terminal and enter the following command:
```shell
npx create-evogram-app my-bot basic-bot
```